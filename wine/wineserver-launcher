#!/bin/sh
set -eu

: "${APPDIR:?错误：缺少 APPDIR。}"

WINE_SERVER="$APPDIR/opt/wine-staging/bin/wineserver"
if [ ! -x "$WINE_SERVER" ]; then
  printf '错误：AppImage 内缺少 wineserver：%s\n' "$WINE_SERVER" >&2
  exit 1
fi

exec "$WINE_SERVER" "$@"
